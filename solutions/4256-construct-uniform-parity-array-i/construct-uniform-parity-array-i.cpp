class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        // if we want odd then we need atleast 2 odds and no of evens>2
        // if we want even odd>2 and even > 2 
        // if there is one odd
        // then t
        // if there is one odd one even we can make 2 odds
        return true;
    }
};